spring.application.name=banking-app
spring.datasource.url=jdbc:mysql://localhost:3306/banking
spring.datasource.username=Junius
spring.datasource.password=darboParc|ja785geriausia
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
logging.level.root=INFO
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss.SSS} WAZZUp [%t] %-5level %logger{36} - %msg%n

# Logback appender for logging to a file
# Note: Spring Boot uses Logback for logging by default
logging.file.name=logs/logback.log
logging.file.max-size=10MB
logging.file.max-history=5

# Logback pattern layout for the file appender
logging.pattern.file=[%-5level] %d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %c{1} - %msg%n

